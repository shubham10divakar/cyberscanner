[project]
name = "sample"
version = "1.0.0"
dependencies = [
    "requests>=2.0",
    "flask==2.0.0",
    "pydantic>=1.10.0",
]

[tool.poetry.dependencies]
python = "^3.8"
django = "^2.2.0"
Pillow = "^8.0.0"
